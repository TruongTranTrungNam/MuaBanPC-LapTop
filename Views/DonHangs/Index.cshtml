@model IEnumerable<MuaBanPC_LapTop.Models.DonHang>

@{
    ViewData["Title"] = "Đơn Hàng";
}

<h1 style="color:orangered">Đơn Hàng của bạn</h1>

<p>
    <a style="text-decoration:none;color:black" asp-action="Create">Tạo mới</a>
</p>

<form asp-controller="DonHangs" asp-action="trangchu1" method="get">
    <p>

        Tên sản phẩm: <input type="text" name="SearchString" />
        <input type="submit" value="Tìm kiếm">
    </p>
</form>


<table class="table">
    <thead style="color:green">
        <tr>
            <th>
                Tên Sản Phẩm
            </th>
            <th>
                Ngày Mua
            </th>
            <th>
                Số Lượng
            </th>
            <th>
                Giá Tiền
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TenSP)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NgayMua)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SoLuong)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GiaTien)
                </td>
                <td>
                    <a style="color: orangered; text-decoration: none" asp-action="Edit" asp-route-id="@item.Id">Sửa</a> |
                    <a style="color: orangered; text-decoration: none" asp-action="Details" asp-route-id="@item.Id">Chi tiết</a> |
                    <a style="color: orangered; text-decoration: none" asp-action="Delete" asp-route-id="@item.Id">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>

